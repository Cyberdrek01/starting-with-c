// type declaration instruction declare var  before using it

#include <stdio.h>
#include <math.h>

int main() {
    int a=15;
    int b= a;
    int c= b+1;
    printf("c=%d\n",c);
    return 0;
}
// arithmetic instruction + - *  / %  to define power use pow e.g: a=pow(b,2)
// comparison instruction == != > >= < <= 
// logic instruction && || !
// bitwise AND | ^ ~
